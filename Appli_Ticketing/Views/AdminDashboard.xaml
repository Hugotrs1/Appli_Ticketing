<Page x:Class="Appli_Ticketing.Views.AdminDashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:Appli_Ticketing.ViewModels"
      Background="#043123">

    <Grid>
        <Image Source="/Assets/backgroundfull.png"
               Stretch="Fill"
               Panel.ZIndex="0"
               Opacity="0.8"/>

        <DockPanel Margin="20" Panel.ZIndex="1">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0 0 0 20" VerticalAlignment="Center">
                <Image Source="/Appli_Ticketing;component/Assets/logo_app.png" Width="100" Height="100" Margin="0 0 10 0"/>
                <TextBlock Text="Gestion des Tickets" FontSize="50" Foreground="#D3C29C" FontWeight="Bold" Margin="0,10,0,0"/>
            </StackPanel>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0"
                          ItemsSource="{Binding Tickets}"
                          SelectedItem="{Binding SelectedTicket, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"         Binding="{Binding Id}"        Width="50"/>
                        <DataGridTextColumn Header="Titre"      Binding="{Binding Title}"     Width="*"/>
                        <DataGridTextColumn Header="Type"       Binding="{Binding Type}"      Width="*"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Utilisateur" Binding="{Binding UserName}" Width="100"/>
                        <DataGridTextColumn Header="Statut"     Binding="{Binding Status}"    Width="100"/>
                        <DataGridTextColumn Header="Création"   Binding="{Binding DateCreation}" Width="*"/>
                        <DataGridTextColumn Header="Réponse"    Binding="{Binding Response}"  Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="Répondre" Height="30" Width="100" Command="{Binding RespondCommand}" Margin="5"/>
                    <Button Content="Mettre en Attente" Height="30" Width="120" Command="{Binding SetOnHoldCommand}" Margin="5"/>
                    <Button Content="Détails" Height="30" Width="100" Command="{Binding DetailCommand}" Margin="5"/>
                    <Button Content="Supprimer" Height="30" Width="100" Command="{Binding DeleteCommand}" Margin="5"/>
                    <Button Content="Se Déconnecter" Width="120" Height="30" Command="{Binding LogoutCommand}" Margin="5"/>
                </StackPanel>
            </Grid>
        </DockPanel>
    </Grid>
</Page>

<Page x:Class="Appli_Ticketing.Views.AdminDashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:Appli_Ticketing.ViewModels"
      Background="#1E293B">

    <DockPanel Margin="20">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0 0 0 20" VerticalAlignment="Center">
            <Image Source="/Appli_Ticketing;component/Assets/logo_app.png" Width="50" Height="50" Margin="0 0 10 0"/>
            <TextBlock Text="Gestion des Tickets (Admin)" FontSize="32" Foreground="White" FontWeight="Bold"/>
        </StackPanel>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DataGrid Grid.Row="0"
                      ItemsSource="{Binding Tickets}"
                      SelectedItem="{Binding SelectedTicket, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"         Binding="{Binding Id}"        Width="50"/>
                    <DataGridTextColumn Header="Titre"      Binding="{Binding Title}"     Width="*"/>
                    <DataGridTextColumn Header="Utilisateur" Binding="{Binding UserId}"   Width="100"/>
                    <DataGridTextColumn Header="Status"     Binding="{Binding Status}"    Width="100"/>
                    <DataGridTextColumn Header="Réponse"    Binding="{Binding Response}"  Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Répondre" Height="30" Width="100" Command="{Binding RespondCommand}" Margin="5"/>
                <Button Content="Mettre en Attente" Height="30" Width="120" Command="{Binding SetOnHoldCommand}" Margin="5"/>
                <Button Content="Valider" Height="30" Width="100" Command="{Binding ValidateCommand}" Margin="5"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Page>
